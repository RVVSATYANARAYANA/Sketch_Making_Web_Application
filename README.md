# Sketch_Making_Web_Application
Sketch Maker — a tiny Flask app that converts uploaded images to pencil sketches (OpenCV), saves the originals and sketches to static/uploads, and provides a simple web UI to download the sketch.
# Sketch Maker

A very small, beginner-friendly Flask app that converts uploaded images into pencil sketches using OpenCV.  
Uploads and generated sketches are saved to `static/uploads`. The UI is intentionally simple so you can understand and extend it.

---

## Demo / Quick test image (local)
If you already have the example image used during development, it is available at this local path on the developer machine:

